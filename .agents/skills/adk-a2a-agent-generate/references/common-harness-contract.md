# Shared Harness Contract

Use a shared harness only as a portability boundary.

The harness may standardize:

- request normalization
- evidence summary shape
- classification JSON shape
- commonization notes shape
- handoff section order
- scaffold file conventions

The harness should not contain private environment assumptions or deployment-specific configuration.
